-- 領収書の宛名。
--
-- これまでは登録名で固定だった。法人利用では「上様」や部署名・正式社名を
-- 入れたい場面があり、そのたびに印刷し直しを頼まれることになる。
--
-- NULL = 未指定（登録名を使う）。お客様ご自身が自分の注文に対してのみ
-- 設定でき、金額には一切影響しない。
ALTER TABLE `orders` ADD `receipt_addressee` text;
